import 'package:flutter/material.dart';

class OpenHouseSchedulePage extends StatelessWidget {
  const OpenHouseSchedulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Open House Schedule'),
      ),
      body: const Center(
        child: Text('Open House Schedule Content Goes Here'),
      ),
    );
  }
}
